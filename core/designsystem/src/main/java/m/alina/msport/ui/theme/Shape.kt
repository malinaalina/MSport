package m.alina.msport.ui.theme

import androidx.compose.foundation.shape.RoundedCornerShape
import androidx.compose.material3.Shapes
import androidx.compose.ui.unit.dp

val BloomRadius = 20.dp

val Shapes = Shapes(
    extraSmall = RoundedCornerShape(BloomRadius),
    small = RoundedCornerShape(BloomRadius),
    medium = RoundedCornerShape(BloomRadius),
    large = RoundedCornerShape(BloomRadius),
    extraLarge = RoundedCornerShape(BloomRadius),
)
